# Oligo Sequence Deconstruction Tool - Requirements
# ================================================
# 
# This application uses mostly Python standard library modules.
# The main dependencies are listed below.
#
# Installation:
#   pip install -r requirements.txt
#
# Note: Tkinter is typically included with Python, but on some Linux systems
# you may need to install it separately:
#   - Ubuntu/Debian: sudo apt-get install python3-tk
#   - Fedora: sudo dnf install python3-tkinter
#   - macOS: usually included with Python from python.org or Homebrew

# No external packages required - all dependencies are from the Python standard library:
# - tkinter (GUI framework)
# - collections (Counter, defaultdict)
# - dataclasses (data containers)
# - typing (type hints)
# - itertools (combinatorics)
# - re (regular expressions)
# - threading (for potential async operations)

# Optional: For development/testing
# pytest>=7.0.0  # Uncomment if you want to run tests
# mypy>=1.0.0    # Uncomment for type checking
